mongodb guarda los datos en binary json (bson)
colecciones = tablas
registros = documentos

documentos tiene una estructura definida.

se recomienda que las colecciones se escriban en plural , ejemplo, "dishes"

mvc @Column en webflux = @field